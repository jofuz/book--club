<style type="text/css">
	.row {
		width: 90%;
		margin-left: 5%;
	}

	.book {
		width: 30%;
		margin-right: 5%;
		display: inline-block;
		margin-bottom: 20px;
		padding: 2%;
		float: left;
		border: 2px solid #EEE;
		border-radius: 5%;
	}

	.book:nth-of-type(3n) {
		margin-right: 0%;
	}

	.book img {
		max-width: 100%;
		max-height: 280px;
	}

	.book:hover {
		-webkit-box-shadow: 4px 4px 10px 0px rgba(204,204,204,1);
        moz-box-shadow: 4px 4px 10px 0px rgba(204,204,204,1);
        box-shadow: 4px 4px 10px 0px rgba(204,204,204,1);
	}
@media screen and (max-width: 768px) {
  .row {
		width: 100%;
		margin-left: 0%;
	}

	.book {
		width: 80%;
		margin-right: 0%;
		margin-left: 10%;
		display: block;
		margin-bottom: 10px;
		padding: 2%;
		float: left;
		border: 2px solid #EEE;
		border-radius: 5%;
	}

	.book img {
		max-width: 100%;
		max-height: 250px;
	}

}

</style>

<div class="row">
	<div class="text-center">
	<h2>Welcome to Book Club!</h2>
	<p>This is a place where you can add some books that you like or have read. Also, other users can leave a review on books.</p>
	<% if not user_signed_in? %>
	<p>You must <%= link_to "Log In", new_user_session_path %> to add a book</p>
	<% end %>
		<h2 class="current-category"><%= params[:category] %></h2>
		<% if @books.count == 0 %>
			<p>There are no books currently in this category</p>
		<% else %>
			<% @books.each do |book| %>
			<div class="book">
			<a href="<%= book_path(book) %>">
			<!--<h3><%= link_to book.title, book_path(book) %></h3>-->
			<img src="<%= book.book_img %>">
			</a>
			</div>
		<% end %>
	<% end %>
	</div>
</div>

